# 09: scripts 编排重组 + 测试一次性换源

**What to build:** 重建入口成为新链路的一键编排（提取增量 → 全量构建 → 事实层落盘 → FTS 重投影），保留既有的锁与接管壳；分析入口瘦身为"读事实层 + 生成报告"（旧数据库适配依赖整体删除）；测试数据一次性换源——旧数据库 fixture 替换为"mini graph.json + FTS 缓存产物"，测试逻辑不变只换数据源，全量测试套件在新链路上绿。

**Blocked by:** 06（点查类工具换源）、07（ranked 换源 + 金标重建）、08（dispatch 概念退役）

**Status:** ready-for-agent

- [ ] 重建入口一键跑通新链路全流程，产物对（事实层 + 缓存）齐备；锁与陈旧接管行为保留
- [ ] 分析入口不再 import 旧数据库适配层；报告/百科生成照旧
- [ ] 状态文件的指纹字段换为事实层时间戳（schema 升版，旧状态文件平滑处理）
- [ ] 旧数据库 fixture 全部替换，不留双 fixture 中间态；全量测试绿
- [ ] 自定义面登记脚本收录全部新增文件
