# 07: ranked 检索换源 + 金标重建

**What to build:** 多通道融合检索（get_ranked_context）的 BM25 通道改查自建 FTS 缓存（schema、列权重、tokenizer 已在 05 逐字平移，排序应等价）；gap 联动通道换 04 产出的原生失败引用源保留语义。检索质量金标在新链路数据上重建，验收阈值 95%（5% 容差覆盖上游提取覆盖度差异）。

**Blocked by:** 05（FTS 缓存模块）、04（resolved_by 打点 + raw_calls 失败收集器）

**Status:** ready-for-agent

- [ ] BM25 通道排序与旧链路等价（同查询同序同分，等价性由缓存构建的列/权重/tokenizer 三同保证）
- [ ] gap 联动通道在新失败引用源上保留（查询词与失败引用匹配语义不变）
- [ ] 融合检索金标在提取契约落地后的新数据上重建，通过率 ≥ 95%
- [ ] 降级路径（缓存不可用时回退纯图通道）行为不变
