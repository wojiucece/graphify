/** Computes the widget's bounding box. */
function computeBounds(widget) {
  return {};
}

/** A legacy widget class. */
class LegacyWidget {
  /** Reset its internal state. */
  reset() {}
}

/** Arrow factory for plain widgets. */
const makeWidget = () => ({});
